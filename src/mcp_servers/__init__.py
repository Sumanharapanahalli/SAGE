"""
SAGE Framework — Built-in MCP Servers
======================================
Provider-agnostic tool modules available to all SAGE agents regardless of
which LLM backend is active (Gemini, Ollama, Claude, local, etc.).

These are loaded by MCPRegistry alongside solution-specific tools.
Solution tools live in solutions/<name>/mcp_servers/.
Framework tools live here in src/mcp_servers/.
"""
