# Core
pyyaml
pydantic

# Observability — Langfuse (optional: set LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY to enable)
langfuse

# LLM - Claude (Anthropic API, optional — needed for provider="claude")
anthropic>=0.40.0

# LLM - Local Provider (optional, only needed for provider="local")
# Install with GPU: pip install llama-cpp-python
# llama-cpp-python>=0.2.77

# RAG / Vector Memory
chromadb
langchain
langchain-community
langchain-chroma
langchain-huggingface
sentence-transformers

# Phase 1 — LlamaIndex RAG backend (optional; set memory.backend: llamaindex)
llama-index-core
llama-index-vector-stores-chroma

# Phase 1 — LangChain tool integrations (already pulled via langchain-community above)

# Phase 1 — mem0 long-term memory (optional; only used if enabled)
mem0ai

# API
fastapi
uvicorn
pydantic

# Utilities
python-dotenv
requests
pandas
numpy

# MCP Framework
fastmcp>=0.1.0

# Phase 3 — LangGraph orchestration (optional; set orchestration.engine: langgraph)
langgraph
langgraph-checkpoint-sqlite

# Phase 4 — AutoGen multi-agent code tasks (optional)
# pip install pyautogen   OR   pip install autogen-agentchat
# pyautogen

# Hardware — Serial Port
pyserial

# Hardware — J-Link Debugger
pylink-square

# Authentication — JWT (OIDC) + API key
python-jose[cryptography]

# HTTP / Async
httpx

# Microsoft Authentication (Teams Graph API)
msal

# Microsoft Teams / Graph API
# (uses requests + msal — no extra SDK required)

# GitLab
# (uses requests — GitLab REST API v4)

# Composio — 100+ pre-built tool integrations for AI agents (optional)
# pip install composio-langchain
# Set COMPOSIO_API_KEY in .env, then: composio add <app>
composio-langchain
