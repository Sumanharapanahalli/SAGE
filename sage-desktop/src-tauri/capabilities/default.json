{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default window + shell permissions for SAGE Desktop.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
