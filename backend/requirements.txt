# ==============================================================================
# Elder Fall Detection — Cloud Backend Requirements
# Python 3.11 | FastAPI + MQTT + PostgreSQL + Redis + HIPAA compliance
# Install: pip install -r requirements.txt
# ==============================================================================

# --- Web framework -----------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# --- Data validation & settings ----------------------------------------------
pydantic==2.7.1
pydantic-settings==2.2.1

# --- Database (PostgreSQL + ORM + migrations) --------------------------------
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# --- Caching -----------------------------------------------------------------
redis==5.0.4

# --- MQTT (device telemetry & alert pipeline) --------------------------------
paho-mqtt==2.0.0

# --- HTTP client -------------------------------------------------------------
httpx==0.27.0

# --- Authentication (JWT + password hashing) ---------------------------------
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Structured logging ------------------------------------------------------
structlog==24.1.0

# --- Observability -----------------------------------------------------------
prometheus-client==0.20.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp==1.24.0

# --- Push notifications (caregiver app FCM) ----------------------------------
firebase-admin==6.5.0

# --- Encryption (HIPAA PHI at rest, AES-256) ---------------------------------
cryptography==42.0.7

# --- Utilities ---------------------------------------------------------------
python-dateutil==2.9.0
pytz==2024.1
orjson==3.10.3
