# ETL Pipeline — pinned dependencies
# Generated for Python 3.11+
# Install: pip install -r requirements.txt

# Database
psycopg2-binary==2.9.9

# Config
PyYAML==6.0.1

# Data processing
pandas==2.2.2
numpy==1.26.4

# Experiment logging / metrics (optional, for ML downstream tasks)
scikit-learn==1.5.0
mlflow==2.13.0

# Observability
structlog==24.2.0
prometheus-client==0.20.0

# Voice pipeline — audio processing (optional; quality metrics only)
# Requires ffmpeg on PATH: https://ffmpeg.org/download.html
pydub==0.25.1

# UGC pipeline — language detection (optional; degrades gracefully if absent)
langdetect==1.0.9

# UGC pipeline — synthetic sample data generation (optional; dev/CI only)
Faker==24.3.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0
pytest-mock==3.14.0
