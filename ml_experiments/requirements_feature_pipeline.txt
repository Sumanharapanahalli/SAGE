# Feature Engineering Pipeline — pinned dependencies
# Install: pip install -r requirements_feature_pipeline.txt

# Core ML
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2          # >= 1.3 required for TargetEncoder

# Experiment tracking (optional — falls back to JSON if absent)
mlflow==2.12.1

# Dev / testing
pytest==8.2.0
