# Core computation — no sklearn for modeling, only for data loading
numpy==1.26.4

# Data loading only: fetch Boston Housing via fetch_openml
# The regression model itself is pure NumPy (no sklearn LinearRegression used)
scikit-learn==1.3.2
