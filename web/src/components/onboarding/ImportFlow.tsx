import React from 'react'

interface ImportFlowProps {
  llmConnected: boolean
}

export default function ImportFlow({ llmConnected: _llmConnected }: ImportFlowProps) {
  return <div style={{ color: '#94a3b8', fontSize: 13 }}>Import flow — coming in Task 7</div>
}
